//  @ts-check

/** @type {import('prettier').Config} */
const config = {
  trailingComma: 'all',
  printWidth: 80,
  tabWidth: 2,
  semi: true,
  singleQuote: true,
  // crlf or lf
  endOfLine: 'auto',
};

export default config;
